#!/bin/bash
# A simple script to set up, build and run the chezmoi project environment

# arguments will include:

# --setup : to set up the environment
# --build : to build the project
# --run   : to run the project
# --clean : to clean up the build artifacts
